FROM node:20-alpine

WORKDIR /var/www/html

# Set environment variables
ENV NODE_ENV=development

# Expose port for Vite development server
EXPOSE 5173

# Keep container running without starting dev server
CMD ["tail", "-f", "/dev/null"]
